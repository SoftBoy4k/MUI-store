

import { Drawer } from '@mui/material';
import React from 'react'

const Basket = (props) => {
    const {
        order,
        removeFromOrder
    } = props;

  return (
    <Drawer>
        
    </Drawer>
  )
}

export default Basket